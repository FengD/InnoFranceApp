# -----------------------------------------------------------------------------
# InnoFranceApp environment variables
# Copy this file to .env and fill in your values:
#   cp env.example .env
# -----------------------------------------------------------------------------

# --- Project and paths ---
# Override repo root (optional). Leave unset for auto-detection.
# INNOFRANCE_PROJECT_ROOT=/path/to/repo

# Run artifacts directory (default: <root>/InnoFranceApp/runs)
INNOFRANCE_RUNS_DIR=InnoFranceApp/runs

# Python command used to start MCP services (default: python3)
# INNOFRANCE_PYTHON_CMD=python3

# --- MCP service directories (relative to project root or absolute) ---
# Paths to each MCP service repo. Omit to use defaults under project root.
# INNOFRANCE_YT_EXTRACTOR_DIR=InnoFranceYTAudioExtractor
# INNOFRANCE_ASR_DIR=InnoFranceASRService
# INNOFRANCE_TRANSLATE_DIR=InnoFranceTranslateAGENT
# INNOFRANCE_TTS_DIR=InnoFranceVoiceGenerateAgent

# --- ASR models (used by InnoFranceASRService) ---
# Whisper model path
# WHISPER_MODEL_PATH=/path/to/whisper/model
# Speaker diarization model path
# DIARIZATION_MODEL_PATH=/path/to/diarization/model

# --- TTS models (used by InnoFranceVoiceGenerateAgent) ---
# Voice design and clone model paths
# VOICE_DESIGN_MODEL_PATH=/path/to/voice_design/model
# VOICE_CLONE_MODEL_PATH=/path/to/voice_clone/model

# --- LLM / translation (used by InnoFranceTranslateAGENT) ---
# OpenAI (for provider=openai)
# OPENAI_API_KEY=sk-your-openai-key

# DeepSeek (for provider=deepseek)
# DEEPSEEK_API_KEY=your-deepseek-key
# DEEPSEEK_API_BASE=https://api.deepseek.com

# --- YouTube (optional; can also be set in config.json per service) ---
# Cookies file path for yt-dlp (e.g. exported from browser)
# YT_COOKIES_FILE=/path/to/cookies.txt
# Custom User-Agent
# YT_USER_AGENT=Mozilla/5.0 ...
# Proxy URL for YouTube extraction
# YT_PROXY=http://proxy:port

# --- S3 storage (optional) ---
# If set, generated audio files are also uploaded to S3-compatible storage.
# INNOFRANCE_S3_ENDPOINT=https://s3.example.com
# INNOFRANCE_S3_BUCKET=your-bucket
# INNOFRANCE_S3_ACCESS_KEY=your-access-key
# INNOFRANCE_S3_SECRET_KEY=your-secret-key
# INNOFRANCE_S3_PREFIX=inno_france


